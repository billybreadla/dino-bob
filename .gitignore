# Local Netlify folder
.netlify

.DS_Store

# 54MB of source/working files for the opening animation. The deployed game
# loads its intro from intro/, not from here, so this is excluded to keep the
# repo lean. Remove this line if you want it tracked too.
Dino Bob's Opening Adventure/

# Art-pipeline source dumps + design concepts (build artifacts, not shipped).
# The already-tracked assets/source/art-overhaul-v5/ masters stay tracked;
# new source dumps (3D prerenders, smooth-arms, etc.) are ignored.
assets/source/
assets/concepts/
